#include "BuilderMadeMaze.h"

//Uncomment if using Builder pattern
BuilderMadeMaze::BuilderMadeMaze()
{
	/*Maze *maze;
	MazeGame game;
	StandardMazeBuilder builder;*/

	//Uncomment if using Builder pattern
	//game.CreateMaze(builder);
	//maze = builder.GetMaze();
}

void BuilderMadeMaze::CountUpMaze()
{
	/*int rooms, doors;
	MazeGame game;
	CountingMazeBuilder builder;*/
	
	//Uncomment if using Builder pattern
	//game.CreateMaze(builder);
	//builder.GetCounts(rooms, doors);

	/*cout << "The Maze has "
		<< rooms << " rooms and "
		<< doors << " doors." << endl;*/
}